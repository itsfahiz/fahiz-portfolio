import 'package:flutter/material.dart';

// Professional and modern color palette
const primaryColor = Color(0xFF00DFA2); // Teal green – vibrant and clean
const secondaryColor = Color(0xFF5B5F97); // Indigo-purple for subtle contrast
const darkColor = Color(0xFF1E1E1E); // Surface color (cards, modals, etc.)
const bodyTextColor = Color(0xFFB0B0B0); // Soft gray for body text
const bgColor = Color(0xFF121212); // Main background
const defaultPadding = 20.0; // Layout spacing
